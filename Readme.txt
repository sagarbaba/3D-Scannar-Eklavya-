This Branch hepls taking .ply file as input and display the Cloud dot representing the 3D object.

Note:
1.If you are using a .ply file in ASCII format, then change "property list uint8 " to "property list float32" in the .ply file.
2.If you are using a .ply file in binary format, then no issues.
